{
  "name": "dex-subgraph",
  "version": "1.0.0",
  "description": "The Graph Subgraph for DEX - Uniswap V2 Fork",
  "repository": "https://github.com/your-username/dex-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ dex-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dex-subgraph",
    "create-local": "graph create --node http://localhost:8020/ dex-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ dex-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.56.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0"
  }
}

